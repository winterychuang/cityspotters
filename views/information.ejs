<!DOCTYPE html>
<html>
        <title> Information </title>
    </head>
    <body>
        
        <header> 
            <%- include('./components/header') %>
            <h1> <%= data.name %> </h1>
            <h4> <%= data.address %> </h4>
            
                <button type="button">
                    <a href = <%= data.link %> > Site </a>
                </button> 
            <p> 
                Save
            <p>
        </header>
        <img src = <%= data.picture %> /> 
        <p>
            <%- include('./components/descbox') %>
        </p>

        <h6>
            <%- include('./components/timetable') %>
        </h6>
        <p>
            check to see if this works -- right now the thought is to have a nested list
            of the places' comments and just pull it using that and then display all of them...?
            probably not doing this right
            <%= comments %>
        </p>
        
    </body>
</html>